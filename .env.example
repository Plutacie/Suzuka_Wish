# 若 Vercel 只给了 KV_* 变量，请抄成下面两行（名字必须一致，代码才读得到）：
#   KV_REST_API_URL   → UPSTASH_REDIS_REST_URL
#   KV_REST_API_TOKEN → UPSTASH_REDIS_REST_TOKEN（用「可写」那把，不要用 READ_ONLY）

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
